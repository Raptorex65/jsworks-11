

let filledArray = Array.from({length:10},()=> ({'hello':'goodbye'}))

-----------

const newArray = notes.map(value => value.map(number => number * 2));
 
console.log(newArray); 



// İki ayrı arrayın elemanlarını yeni bir array içinde birleştirme
var allusers = users.concat(users2);


